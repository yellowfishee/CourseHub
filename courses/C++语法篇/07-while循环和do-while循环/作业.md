# while循环和do-while循环 - 作业

---

## 选择题

**1.** `while` 循环和 `do-while` 循环的主要区别是：

A. 没有区别

B. `do-while` 至少执行一次

C. `while` 至少执行一次

D. `while` 更快

---

**2.** `do-while` 循环的格式是：

A. `do { 循环体 } while(条件);`

B. `while { 循环体 } do(条件);`

C. `do while(条件) { 循环体 }`

D. `loop { 循环体 } until(条件);`

---

**3.** `while(n > 1)` 循环体执行的条件是：

A. n 大于 1

B. n 小于 1

C. n 等于 1

D. n 不等于 1

---

**4.** `n /= 2` 等价于：

A. n = n / 2

B. n = 2 / n

C. n = n - 2

D. n = 2

---

**5.** 在 3n+1 问题中 `count++` 的作用是：

A. 计算 3n+1

B. 计数器，记录变换次数

C. 判断是否结束

D. 输入数据

---

**6.** `while` 循环可以用 `for` 循环改写吗？

A. 不可以

B. 只有特定情况可以

C. 可以互相转换

D. 取决于编译器

---

**7.** `do-while` 适用于什么场景？

A. 循环次数已知

B. 循环体至少需要执行一次

C. 需要先判断后执行

D. 需要嵌套循环

---

**8.** `while(1)` 和 `for(;;)` 都是死循环吗？

A. 不是

B. 是的

C. `while(1)` 不是

D. `for(;;)` 不是

---

## 填空题

**9.** `while` 循环的格式：while（________）循环体；

**10.** `do-while` 循环的特点是循环体至少执行________次。

**11.** `n++` 和 `++n` 都会给 n 加1，但当用在一个表达式中时，n++ 使用加1______的n值。

**12.** `while(scanf("%d", &x) == 1)` 用于处理________数量的输入。

**13.** 在 3n+1 问题中，如果 n 是奇数，变换规则是 n = ________。

---

## 编程题

**基础题**

**题目1：** 使用 `while` 循环输出 1 到 100 的整数。

```
示例输出：1 2 3 ... 99 100
```

---

**题目2：** 使用 `while` 循环计算 1+2+3+...+n 的和。

```
示例输入：10
示例输出：55
```

---

**题目3：** 输入一个正整数 n，使用 `while` 循环计算 n!。

```
示例输入：5
示例输出：120
```

---

**题目4：** 使用 `do-while` 循环，实现一个简单的猜数字游戏：程序随机一个 1-100 的数，让用户猜，提示"太大"或"太小"，直到猜对。

```
示例输入/输出：
请猜一个1-100的数：50
太小了！
请猜一个1-100的数：75
太大了！
请猜一个1-100的数：63
恭喜你猜对了！
```

---

## 原书例题与习题

**例题2-2　3n+1问题【原书2.2节】**

猜想：对于任意大于1的自然数 n，若 n 为奇数，则将 n 变为 3n+1，否则变为 n 的一半。经过若干次这样的变换，一定会使 n 变为1。例如，3→10→5→16→8→4→2→1。

输入 n，输出变换的次数。n ≤ 10⁹。

```
示例输入：3
示例输出：7
```

---

**例题2-3　近似计算【原书2.2节】**

计算 π/4 = 1 - 1/3 + 1/5 - 1/7 + ...，直到最后一项小于 10⁻⁶。

```
示例输出：0.785147（保留6位小数）
```

---

**思考题：while vs for**

下面程序的运行结果是什么？"!=" 表示"不相等"。提示：请上机实验，不要凭主观感觉回答。

```c
#include <stdio.h>
int main() {
    double i;
    for(i = 0; i != 10; i += 0.1)
        printf("%.1f\n", i);
    return 0;
}
```

---

## 参考答案

**选择题：** 1-B, 2-A, 3-A, 4-A, 5-B, 6-C, 7-B, 8-B

**填空题：**
9. 条件
10. 一/1
11. 前/旧/原来
12. 不确定/未知
13. 3n+1

**编程题（参考答案）：**

```c
// 题目1
#include <stdio.h>
int main() {
    int i = 1;
    while(i <= 100) {
        printf("%d ", i);
        i++;
    }
    printf("\n");
    return 0;
}

// 题目2
#include <stdio.h>
int main() {
    int n, sum = 0, i = 1;
    scanf("%d", &n);
    while(i <= n) {
        sum += i;
        i++;
    }
    printf("%d\n", sum);
    return 0;
}

// 题目3
#include <stdio.h>
int main() {
    int n;
    long long fact = 1;
    scanf("%d", &n);
    int i = 1;
    while(i <= n) {
        fact *= i;
        i++;
    }
    printf("%lld\n", fact);
    return 0;
}

// 题目4
#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int main() {
    srand(time(NULL));
    int target = rand() % 100 + 1;
    int guess;
    
    do {
        printf("请猜一个1-100的数：");
        scanf("%d", &guess);
        if(guess > target) {
            printf("太大了！\n");
        } else if(guess < target) {
            printf("太小了！\n");
        }
    } while(guess != target);
    
    printf("恭喜你猜对了！\n");
    return 0;
}

// 例题2-2 3n+1问题
#include <stdio.h>
int main() {
    long long n;
    int count = 0;
    scanf("%lld", &n);
    while(n > 1) {
        if(n % 2 == 1) {
            n = n * 3 + 1;
        } else {
            n /= 2;
        }
        count++;
    }
    printf("%d\n", count);
    return 0;
}

// 例题2-3 近似计算
#include <stdio.h>
int main() {
    double sum = 0;
    for(int i = 0; ; i++) {
        double term = 1.0 / (i * 2 + 1);
        if(i % 2 == 0) sum += term;
        else sum -= term;
        if(term < 1e-6) break;
    }
    printf("%.6f\n", sum);
    return 0;
}

// 思考题答案
// 这个程序可能会陷入死循环或提前退出，因为浮点数精度问题
// 0.1 不能被精确表示，累加后可能不等于 10
// 建议使用整数循环变量，或者用 i <= 10 判断

```

---
