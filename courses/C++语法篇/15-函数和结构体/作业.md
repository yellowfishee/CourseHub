# 函数和结构体 - 作业

---

## 选择题

**1.** 函数的返回类型void表示：

A. 返回整型

B. 返回空（没有返回值）

C. 返回字符

D. 返回浮点数

---

**2.** 如何正确声明一个返回两个整数最大值的函数？

A. max(a, b)

B. int max(int a, b)

C. int max(int a, int b)

D. void max(int a, int b)

---

**3.** 结构体成员用什么符号访问？

A. ->

B. .

C. :

D. ,

---

**4.** 以下结构体定义正确的是：

A. struct Student { name[50]; }

B. struct Student { char name[50]; }

C. struct Student { name: char[50]; }

D. struct Student { (name); }

---

**5.** 函数调用时，实参和形参的关系是：

A. 形参改变会影响实参

B. 形参是实参的副本（值传递）

C. 没有任何关系

D. 实参是形参的副本

---

**6.** 下列关于函数的说法错误的是：

A. 函数可以没有参数

B. 函数可以没有返回值

C. 函数必须先定义才能调用

D. 函数只能返回一个值

---

**7.** 结构体中的成员可以是：

A. 只能基本数据类型

B. 只能数组

C. 可以是基本类型、数组、甚至是其他结构体

D. 只能是字符

---

**8.** 下列哪个不是函数的特点：

A. 提高代码复用性

B. 使程序结构更清晰

C. 增加代码长度

D. 方便调试和维护

---

## 填空题

**9.** 函数的返回类型如果是void，表示__________。

**10.** 结构体定义用__________关键字。

**11.** 访问结构体成员用__________符号。

**12.** 函数名通常使用__________（动词/名词）。

**13.** struct Point { int x, y; } 定义了一个名为__________的结构体类型。

**14.** 函数参数传递默认是__________传递（值/引用）。

---

## 编程题

**基础题**

**题目1：** 编写一个函数，计算两个整数的和。

```
示例输入：3 5
示例输出：8
```

---

**题目2：** 编写一个函数，接收一个Student结构体，打印学生信息。

```
示例输入：小明 15 95.5
示例输出：小明 15 95.5
```

---

**题目3：** 编写一个函数，接收两个Point结构体，计算它们之间的距离。

```
示例输入：0 0
         3 4
示例输出：5.00
```

---

## 原书习题

**习题5-1　判断素数【原书5.1节】**

编写函数判断一个数是否是素数。

```
示例输入：17
示例输出：YES
```

---

**习题5-2　求最大公约数【原书5.1节】**

编写函数用欧几里得算法求两个数的最大公约数。

```
示例输入：12 18
示例输出：6
```

---

## 参考答案

**选择题：** 1-B, 2-C, 3-B, 4-B, 5-B, 6-C, 7-C, 8-C

**填空题：**
9. 没有返回值
10. struct
11. .
12. 动词
13. Point
14. 值

**编程题（参考答案）：**

```c
// 题目1：求和函数
#include <stdio.h>
int sum(int a, int b) {
    return a + b;
}
int main() {
    int x, y;
    scanf("%d %d", &x, &y);
    printf("%d\n", sum(x, y));
    return 0;
}

// 题目2：打印学生信息
#include <stdio.h>
struct Student {
    char name[50];
    int age;
    double score;
};
void printStudent(struct Student s) {
    printf("%s %d %.1f\n", s.name, s.age, s.score);
}
int main() {
    struct Student s;
    scanf("%s %d %lf", s.name, &s.age, &s.score);
    printStudent(s);
    return 0;
}

// 题目3：计算两点距离
#include <stdio.h>
#include <math.h>
struct Point {
    int x, y;
};
double distance(struct Point p1, struct Point p2) {
    int dx = p1.x - p2.x;
    int dy = p1.y - p2.y;
    return sqrt(dx*dx + dy*dy);
}
int main() {
    struct Point p1, p2;
    scanf("%d %d", &p1.x, &p1.y);
    scanf("%d %d", &p2.x, &p2.y);
    printf("%.2f\n", distance(p1, p2));
    return 0;
}

// 习题5-1 判断素数
#include <stdio.h>
int isPrime(int n) {
    if(n < 2) return 0;
    for(int i = 2; i*i <= n; i++)
        if(n % i == 0) return 0;
    return 1;
}
int main() {
    int n;
    scanf("%d", &n);
    printf(isPrime(n) ? "YES\n" : "NO\n");
    return 0;
}

// 习题5-2 最大公约数
#include <stdio.h>
int gcd(int a, int b) {
    if(b == 0) return a;
    return gcd(b, a % b);
}
int main() {
    int a, b;
    scanf("%d %d", &a, &b);
    printf("%d\n", gcd(a, b));
    return 0;
}
```

---
