# 注解与习题 - 作业

---

## 选择题

**1.** C语言中，注释 `/* ... */` 可以嵌套吗？

A. 可以

B. 不可以

C. 取决于编译器

D. 只有C99可以

---

**2.** scanf 读取 double 类型的变量应该使用什么格式符？

A. %f

B. %lf

C. %d

D. %lf 和 %f 都可以

---

**3.** printf 输出 double 类型应该使用什么格式符？

A. %f

B. %lf

C. %d

D. %lf 和 %f 都可以

---

**4.** 赋值表达式 `a = b = 3` 的执行顺序是：

A. 从左到右

B. 从右到左

C. 同时执行

D. 不确定

---

**5.** `int a = 2147483647; a = a + 1;` 之后，a 的值是：

A. 2147483648

B. -2147483648

C. 0

D. 不确定

---

**6.** 整数除法 `5 / 2` 的结果是：

A. 2.5

B. 2

C. 3

D. 2.0

---

**7.** 强制类型转换 `(int)3.14159` 的结果是：

A. 3.14159

B. 3

C. 4

D. 3.0

---

**8.** scanf 中使用 & 的作用是：

A. 表示按位与运算

B. 取变量地址

C. 引用传递

D. 结束输入

---

## 填空题

**9.** C89/C90 是C语言的第_________个ANSI标准。

**10.** scanf("%d", &x) 中，&x 表示 x 的_________。

**11.** printf("%.2f", 3.14159) 输出_________（保留2位小数）。

**12.** 整数除法和浮点数除法的区别在于运算数的_________不同。

**13.** 强制类型转换不会改变_________变量的值。

---

## 编程题

**基础题**

**题目1：** 编写程序，输入两个整数 a 和 b，交换它们的值后输出。

```
示例输入：3 5
示例输出：5 3
```

---

**题目2：** 编写程序，输入一个华氏温度 F，输出对应的摄氏温度 C。
公式：C = (F - 32) * 5 / 9

```
示例输入：100
示例输出：37.78（保留2位小数）
```

---

**题目3：** 编写程序，输入一个三位数，输出它的各位数字之和。

```
示例输入：123
示例输出：6（1+2+3=6）
```

---

**题目4：** 编写程序，输入一个字符，输出它的ASCII码值。

```
示例输入：a
示例输出：97
```

---

## 原书习题

**习题1-1　平均数（average）【原书1.2节】**

输入3个整数，输出它们的平均值，保留3位小数。

```
示例输入：3 4 5
示例输出：4.000
```

---

**习题1-2　温度（temperature）【原书1.2节】**

输入一个华氏温度，输出摄氏温度。保留3位小数。

```
示例输入：212
示例输出：100.000
```

---

**习题1-3　连续和（sum）【原书1.2节】**

输入 a 和 b，输出 a + (a+1) + ... + b 的和。

```
示例输入：5 10
示例输出：45
```

---

**习题1-4　鸡兔同笼（ CRS ）【原书1.4节】**

已知鸡和兔的总数量为 n，总腿数为 m。输入 n 和 m，依次输出鸡的数目和兔的数目。如果无解，则输出 "No answer"。

```
示例输入1：14 32
示例输出1：12 2

示例输入2：10 16
示例输出2：No answer
```

---

**习题1-5　三整数排序（sort）【原书1.4节】**

输入3个整数，从小到大排序后输出。

```
示例输入：20 7 33
示例输出：7 20 33
```

---

**习题1-6　三角形（triangle）【原书1.4节】**

输入三角形3条边的长度值（均为正整数），判断是否能为直角三角形的3个边长。如果可以，则输出 "yes"，如果不能，则输出 "no"。如果根本无法构成三角形，则输出 "not a triangle"。

```
示例输入1：3 4 5
示例输出1：yes

示例输入2：3 3 5
示例输出2：no

示例输入3：1 2 3
示例输出3：not a triangle
```

---

**习题1-7　年份（year）【原书1.4节】**

输入年份，判断是否为闰年。如果是，则输出 "yes"，否则输出 "no"。

提示：简单地判断除以4的余数是不够的。

```
示例输入1：2024
示例输出1：yes

示例输入2：2023
示例输出2：no

示例输入3：2000
示例输出3：yes

示例输入4：1900
示例输出4：no
```

---

## 参考答案

**选择题：** 1-B, 2-B, 3-A, 4-B, 5-B, 6-B, 7-B, 8-B

**填空题：**
9. 一/1
10. 地址
11. 3.14
12. 类型
13. 原

**编程题（参考答案）：**

```c
// 题目1：交换两个变量
#include <stdio.h>
int main() {
    int a, b, t;
    scanf("%d%d", &a, &b);
    t = a;
    a = b;
    b = t;
    printf("%d %d\n", a, b);
    return 0;
}

// 题目2：华氏转摄氏
#include <stdio.h>
int main() {
    double f, c;
    scanf("%lf", &f);
    c = (f - 32) * 5 / 9;
    printf("%.2f\n", c);
    return 0;
}

// 题目3：各位数字之和
#include <stdio.h>
int main() {
    int n, sum = 0;
    scanf("%d", &n);
    sum += n / 100;      // 百位
    sum += n / 10 % 10;   // 十位
    sum += n % 10;        // 个位
    printf("%d\n", sum);
    return 0;
}

// 题目4：ASCII码值
#include <stdio.h>
int main() {
    char c;
    scanf("%c", &c);
    printf("%d\n", (int)c);
    return 0;
}

// 习题1-1 平均数
#include <stdio.h>
int main() {
    double a, b, c;
    scanf("%lf%lf%lf", &a, &b, &c);
    printf("%.3f\n", (a + b + c) / 3.0);
    return 0;
}

// 习题1-2 温度
#include <stdio.h>
int main() {
    double f;
    scanf("%lf", &f);
    printf("%.3f\n", (f - 32) * 5 / 9);
    return 0;
}

// 习题1-3 连续和
#include <stdio.h>
int main() {
    int a, b, sum = 0;
    scanf("%d%d", &a, &b);
    for(int i = a; i <= b; i++) {
        sum += i;
    }
    printf("%d\n", sum);
    return 0;
}

// 习题1-4 鸡兔同笼
#include <stdio.h>
int main() {
    int n, m, chicken, rabbit;
    scanf("%d%d", &n, &m);
    chicken = (4 * n - m) / 2;
    rabbit = n - chicken;
    if(m % 2 == 1 || chicken < 0 || rabbit < 0) {
        printf("No answer\n");
    } else {
        printf("%d %d\n", chicken, rabbit);
    }
    return 0;
}

// 习题1-5 三整数排序
#include <stdio.h>
int main() {
    int a, b, c, t;
    scanf("%d%d%d", &a, &b, &c);
    if(a > b) { t = a; a = b; b = t; }
    if(a > c) { t = a; a = c; c = t; }
    if(b > c) { t = b; b = c; c = t; }
    printf("%d %d %d\n", a, b, c);
    return 0;
}

// 习题1-6 三角形
#include <stdio.h>
int main() {
    int a, b, c;
    scanf("%d%d%d", &a, &b, &c);
    // 判断能否构成三角形
    if(a + b <= c || a + c <= b || b + c <= a) {
        printf("not a triangle\n");
    } else {
        // 判断直角三角形
        int max = a;
        if(b > max) max = b;
        if(c > max) max = c;
        int x, y;
        if(max == a) { x = b; y = c; }
        else if(max == b) { x = a; y = c; }
        else { x = a; y = b; }
        if(max * max == x * x + y * y) {
            printf("yes\n");
        } else {
            printf("no\n");
        }
    }
    return 0;
}

// 习题1-7 年份
#include <stdio.h>
int main() {
    int year;
    scanf("%d", &year);
    if((year % 4 == 0 && year % 100 != 0) || (year % 400 == 0)) {
        printf("yes\n");
    } else {
        printf("no\n");
    }
    return 0;
}
```

---
