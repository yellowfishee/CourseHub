# 递归 - 作业

---

## 选择题

**1.** 递归必须具备什么？

A. 循环

B. 终止条件和递归公式

C. 指针

D. 数组

---

**2.** factorial(5) 的返回值是多少？

A. 20

B. 60

C. 120

D. 24

---

**3.** 递归函数的调用顺序是：

A. 先进先出

B. 后进后出

C. 后进先出

D. 随机顺序

---

**4.** 递归如果没有终止条件会：

A. 程序正常结束

B. 返回0

C. 无限递归导致栈溢出

D. 自动添加终止条件

---

**5.** fib(5) 的返回值是多少？

A. 3

B. 5

C. 8

D. 13

---

## 填空题

**6.** 递归的两个要素是__________和__________。

**7.** factorial(4) = 4 × __________ × 2 × 1。

**8.** 递归调用使用__________（后进先出/先进先出）的栈管理。

**9.** 递归本质上是将大问题分解为__________问题。

**10.** 递归可以用__________来理解（数学归纳法/迭代法）。

---

## 编程题

**题目1：** 编写递归函数计算 1+2+3+...+n。

```
示例输入：5
示例输出：15
```

---

**题目2：** 编写递归函数判断字符串长度。

```
示例输入：Hello
示例输出：5
```

---

**题目3：** 编写递归函数实现字符串逆序输出。

```
示例输入：Hello
示例输出：olleH
```

---

## 原书习题

**习题6-1　求最大公约数【原书6.1节】**

用递归实现欧几里得算法。

```
示例输入：12 18
示例输出：6
```

---

**习题6-2　Ackermann函数【原书6.1节】**

实现 Ackermann 函数。

```
示例输入：3 3
示例输出：29
```

---

## 参考答案

**选择题：** 1-B, 2-C, 3-C, 4-C, 5-B

**填空题：**
6. 终止条件、递归公式
7. 3
8. 后进先出
9. 相同类型的更小
10. 数学归纳法

**编程题答案：**

```c
// 题目1：求和
int sum(int n) {
    if(n == 1) return 1;
    return n + sum(n-1);
}

// 题目2：字符串长度
int strLen(char *s) {
    if(*s == '\0') return 0;
    return 1 + strLen(s+1);
}

// 题目3：逆序输出
void reversePrint(char *s) {
    if(*s == '\0') return;
    reversePrint(s+1);
    putchar(*s);
}

// 习题6-1：最大公约数
int gcd(int a, int b) {
    if(b == 0) return a;
    return gcd(b, a % b);
}

// 习题6-2：Ackermann函数
int ack(int m, int n) {
    if(m == 0) return n + 1;
    if(n == 0) return ack(m-1, 1);
    return ack(m-1, ack(m, n-1));
}
```

---
