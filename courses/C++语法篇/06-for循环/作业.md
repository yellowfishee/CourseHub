# for循环 - 作业

---

## 选择题

**1.** `for(int i = 1; i <= 10; i++)` 循环体执行多少次？

A. 9次

B. 10次

C. 11次

D. 无限次

---

**2.** `for`循环中，`i++` 和 `++i` 的区别是：

A. 没有区别

B. `i++` 先使用后加1，`++i` 先加1后使用

C. `i++` 用于for循环，`++i` 用于其他循环

D. `++i` 更高效

---

**3.** 以下哪个是死循环？

A. `for(int i = 0; i < 10; i++)`

B. `for(int i = 0; i >= 0; i++)`

C. `for(int i = 10; i > 0; i--)`

D. `for(int i = 0; i < 10; i+=2)`

---

**4.** `for`循环的三个部分（初始化；条件；调整）都可以省略吗？

A. 都不可以省略

B. 只有条件可以省略

C. 都可以省略

D. 只有初始化和调整可以省略

---

**5.** 在 `for(int i = 1; i <= n; i++)` 中，变量 i 的作用域是：

A. 整个程序

B. for循环外部

C. for循环内部

D. main函数内部

---

**6.** `continue` 语句的作用是：

A. 退出整个循环

B. 跳回循环开始，执行调整语句并判断条件

C. 暂停循环

D. 结束程序

---

**7.** `break` 语句的作用是：

A. 跳回循环开始

B. 跳过本次循环的剩余语句

C. 退出整个循环

D. 退出程序

---

**8.** `for`循环和 `while` 循环的关系是：

A. 完全没有关系

B. `for`循环是`while`循环的特例

C. 可以互相转换

D. `while`循环更快

---

## 填空题

**9.** `for`循环的标准格式：for（____________；条件；调整）循环体；

**10.** `i += 2` 等价于__________。

**11.** `i *= 2` 等价于__________。

**12.** `for`循环中，如果条件永远为真，会产生__________循环。

**13.** 在嵌套循环中，`break` 只退出__________（最内层/最外层）循环。

---

## 编程题

**基础题**

**题目1：** 使用 `for` 循环输出 1 到 100 的所有整数。

```
示例输出：1 2 3 ... 99 100
```

---

**题目2：** 输入 n，使用 `for` 循环计算 1+2+3+...+n 的和。

```
示例输入：10
示例输出：55
```

---

**题目3：** 输入 n，使用 `for` 循环计算 n!（阶乘）。

```
示例输入：5
示例输出：120
```

---

**题目4：** 输入 n，使用 `for` 循环输出所有的奇数（1, 3, 5, ..., n 或 n-1）。

```
示例输入：9
示例输出：1 3 5 7 9
```

---

## 原书例题与习题

**例题2-1　aabb【原书2.1节】**

输出所有形如 aabb 的4位完全平方数（即前两位数字相等，后两位数字也相等）。

```
示例输出：
7744
```

提示：枚举个位和十位，用 `sqrt` 判断完全平方数。

---

**习题2-1　水仙花数（daffodil）【原书2.5节】**

输出 100~999 中的所有水仙花数。

```
示例输出：
153
370
371
407
```

---

**思考题：修改程序2-1**

题目：假设需要输出 2, 4, 6, 8, ..., 2n，每个占一行。

任务1：修改第7行（`printf` 语句），不修改第6行（for 语句）。

任务2：修改第6行，不修改第7行。

---

## 参考答案

**选择题：** 1-B, 2-A, 3-B, 4-C, 5-C, 6-B, 7-C, 8-C

**填空题：**
9. 初始化
10. i = i + 2
11. i = i * 2
12. 死/无限
13. 最内层

**编程题（参考答案）：**

```c
// 题目1
#include <stdio.h>
int main() {
    for(int i = 1; i <= 100; i++) {
        printf("%d ", i);
    }
    printf("\n");
    return 0;
}

// 题目2
#include <stdio.h>
int main() {
    int n, sum = 0;
    scanf("%d", &n);
    for(int i = 1; i <= n; i++) {
        sum += i;
    }
    printf("%d\n", sum);
    return 0;
}

// 题目3
#include <stdio.h>
int main() {
    int n;
    long long fact = 1;
    scanf("%d", &n);
    for(int i = 1; i <= n; i++) {
        fact *= i;
    }
    printf("%lld\n", fact);
    return 0;
}

// 题目4
#include <stdio.h>
int main() {
    int n;
    scanf("%d", &n);
    for(int i = 1; i <= n; i += 2) {
        printf("%d ", i);
    }
    printf("\n");
    return 0;
}

// 例题2-1 aabb
#include <stdio.h>
#include <math.h>
int main() {
    for(int a = 1; a <= 9; a++) {
        for(int b = 0; b <= 9; b++) {
            int n = a * 1100 + b * 11;
            int m = (int)(sqrt(n) + 0.5);
            if(m * m == n) {
                printf("%d\n", n);
            }
        }
    }
    return 0;
}

// 习题2-1 水仙花数
#include <stdio.h>
int main() {
    for(int i = 100; i <= 999; i++) {
        int a = i / 100;
        int b = i / 10 % 10;
        int c = i % 10;
        if(a*a*a + b*b*b + c*c*c == i)
            printf("%d\n", i);
    }
    return 0;
}

// 思考题答案
// 任务1：printf("%d\n", i*2);
// 任务2：for(int i=1; i<=n; i++)

```

---
