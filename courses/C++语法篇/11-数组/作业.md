# 数组 - 作业

---

## 选择题

**1.** 数组 int a[100] 的有效下标范围是：

A. 1 到 100

B. 0 到 99

C. 0 到 100

D. 1 到 99

---

**2.** 数组声明时，大小可以是：

A. 变量

B. 常量或常量表达式

C. 任意整数

D. 浮点数

---

**3.** 大数组应该声明在哪里？

A. main函数内部

B. main函数外部（全局）

C. 任意位置

D. 头文件中

---

**4.** memset(a, 0, sizeof(a)) 的作用是：

A. 给数组赋值

B. 复制数组

C. 清零数组

D. 求数组长度

---

**5.** memcpy(b, a, sizeof(a)) 的作用是：

A. 比较两个数组

B. 清零数组

C. 把数组a复制到数组b

D. 连接两个数组

---

**6.** 二维数组 int a[5][10] 共有多少个元素？

A. 15

B. 50

C. 55

D. 10

---

**7.** 数组下标从多少开始？

A. 0

B. 1

C. 由程序员决定

D. 由编译器决定

---

**8.** 在C语言中，数组名代表：

A. 第一个元素的地址

B. 整个数组

C. 数组的大小

D. 数组的类型

---

## 填空题

**9.** 数组的大小必须使用__________来声明。

**10.** 数组 a[10] 的第5个元素是__________（填下标）。

**11.** memset 函数包含在__________头文件中。

**12.** 二维数组可以看作是一个__________（行/列）。

**13.** 把数组 a 全部复制到数组 b，需要使用__________函数。

**14.** 数组在内存中是连续存放的，第0个元素的后面是第__________个元素。

---

## 编程题

**基础题**

**题目1：** 声明一个数组，存入5个整数，然后逆序输出。

```
示例输入：1 2 3 4 5
示例输出：5 4 3 2 1
```

---

**题目2：** 在数组中找最大值和最小值。

```
示例输入：5
         3 8 1 9 2
示例输出：max=9 min=1
```

---

**题目3：** 计算数组元素的和与平均值。

```
示例输入：5
         10 20 30 40 50
示例输出：sum=150 avg=30.00
```

---

**题目4：** 统计数组中正数的个数。

```
示例输入：6
         -1 2 0 -3 4 -5
示例输出：3
```

---

## 原书习题

**习题3-1　逆序输出【原书3.1节】**

读入一些整数，逆序输出到一行中。已知整数不超过100个。

```
示例输入：1 2 3 4 5
示例输出：5 4 3 2 1
```

---

**习题3-2　开灯问题【原书3.1节】**

有n盏灯，编号为1～n。第1个人把所有灯打开，第2个人按下所有编号为2的倍数的开关（这些灯将被关掉），第3个人按下所有编号为3的倍数的开关，依此类推。一共有k个人，问最后有哪些灯开着？

```
示例输入：7 3
示例输出：1 5 6 7
```

---

**习题3-3　蛇形填数【原书3.1节】**

在n×n方阵里填入1, 2, ..., n×n，要求填成蛇形。

```
示例输入：4

示例输出：
 10 11 12  1
  9 16 13  2
  8 15 14  3
  7  6  5  4
```

---

## 参考答案

**选择题：** 1-B, 2-B, 3-B, 4-C, 5-C, 6-B, 7-A, 8-A

**填空题：**
9. 常量/常量表达式
10. a[4]
11. string.h
12. 行
13. memcpy
14. 1

**编程题（参考答案）：**

```c
// 题目1：逆序输出
#include <stdio.h>
#define MAXN 105
int main() {
    int a[MAXN], n = 0, x;
    while(scanf("%d", &x) == 1 && n < 100)
        a[n++] = x;
    for(int i = n - 1; i > 0; i--)
        printf("%d ", a[i]);
    printf("%d\n", a[0]);
    return 0;
}

// 题目2：最大值最小值
#include <stdio.h>
int main() {
    int n, a[100], max, min;
    scanf("%d", &n);
    for(int i = 0; i < n; i++) scanf("%d", &a[i]);
    max = min = a[0];
    for(int i = 1; i < n; i++) {
        if(a[i] > max) max = a[i];
        if(a[i] < min) min = a[i];
    }
    printf("max=%d min=%d\n", max, min);
    return 0;
}

// 题目3：和与平均值
#include <stdio.h>
int main() {
    int n, a[100], sum = 0;
    scanf("%d", &n);
    for(int i = 0; i < n; i++) {
        scanf("%d", &a[i]);
        sum += a[i];
    }
    printf("sum=%d avg=%.2f\n", sum, (double)sum / n);
    return 0;
}

// 题目4：统计正数
#include <stdio.h>
int main() {
    int n, a[100], cnt = 0;
    scanf("%d", &n);
    for(int i = 0; i < n; i++) scanf("%d", &a[i]);
    for(int i = 0; i < n; i++)
        if(a[i] > 0) cnt++;
    printf("%d\n", cnt);
    return 0;
}

// 习题3-1 逆序输出
#include <stdio.h>
#define MAXN 105
int a[MAXN];
int main() {
    int x, n = 0;
    while(scanf("%d", &x) == 1)
        a[n++] = x;
    for(int i = n-1; i >= 1; i--)
        printf("%d ", a[i]);
    printf("%d\n", a[0]);
    return 0;
}

// 习题3-2 开灯问题
#include <stdio.h>
#include <string.h>
#define MAXN 1010
int a[MAXN];
int main() {
    int n, k, first = 1;
    scanf("%d%d", &n, &k);
    memset(a, 0, sizeof(a));
    for(int i = 1; i <= k; i++)
        for(int j = 1; j <= n; j++)
            if(j % i == 0) a[j] = !a[j];
    for(int i = 1; i <= n; i++)
        if(a[i]) {
            if(first) first = 0;
            else printf(" ");
            printf("%d", i);
        }
    printf("\n");
    return 0;
}

// 习题3-3 蛇形填数
#include <stdio.h>
#include <string.h>
#define MAXN 20
int a[MAXN][MAXN];
int main() {
    int n, x, y, tot = 0;
    scanf("%d", &n);
    memset(a, 0, sizeof(a));
    x = 0; y = n - 1;
    tot = a[x][y] = 1;
    while(tot < n * n) {
        while(x + 1 < n && !a[x+1][y]) a[++x][y] = ++tot;
        while(y - 1 >= 0 && !a[x][y-1]) a[x][--y] = ++tot;
        while(x - 1 >= 0 && !a[x-1][y]) a[--x][y] = ++tot;
        while(y + 1 < n && !a[x][y+1]) a[x][++y] = ++tot;
    }
    for(x = 0; x < n; x++) {
        for(y = 0; y < n; y++)
            printf("%3d", a[x][y]);
        printf("\n");
    }
    return 0;
}
```

---
