# 注解与习题 - 函数和类复习

---
title: 注解与习题
description: 函数基础回顾、类初探、练习题
prerequisites: 函数和类
---

## 学习目标

- 巩固**函数**的知识
- 初步了解**类**的概念
- 完成综合练习

---

## 内容

### 一、函数要点回顾

**定义函数**：

```python
def 函数名(参数):
    函数体
    return 返回值
```

**例子**：

```python
def add(a, b):
    return a + b

result = add(3, 5)
print(result)  # 8
```

**参数类型**：

```python
# 默认参数
def greet(name, greeting="你好"):
    return f"{greeting}, {name}!"

print(greet("小明"))        # 你好, 小明!
print(greet("小明", "Hello")) # Hello, 小明!

# 可变参数
def sum_all(*nums):
    total = 0
    for n in nums:
        total += n
    return total

print(sum_all(1, 2, 3, 4))  # 10
```

---

### 二、类要点回顾

**定义类**：

```python
class 类名:
    def __init__(self, 参数):
        self.属性 = 参数
    
    def 方法(self):
        # 方法体
```

**例子**：

```python
class Student:
    def __init__(self, name, age):
        self.name = name
        self.age = age
    
    def introduce(self):
        print(f"我叫{self.name}, 今年{self.age}岁")

stu = Student("小明", 12)
stu.introduce()  # 我叫小明, 今年12岁
```

---

### 三、常见错误

**错误1：忘记return**

```python
def add(a, b):
    print(a + b)  # 只是打印，没有返回

x = add(1, 2)  # x 是 None！
```

**正确**：

```python
def add(a, b):
    return a + b

x = add(1, 2)  # x 是 3
```

**错误2：忘记self**

```python
class Student:
    def introduce():  # 错误！缺少 self
        pass
```

**正确**：

```python
class Student:
    def introduce(self):  # 正确
        pass
```

**错误3：调用函数忘记括号**

```python
def say_hello():
    print("Hello")

say_hello  # 只是引用，没有调用
say_hello()  # 正确，加了括号才调用
```

---

### 四、练习题

**题目1：写一个求和函数**

```python
def my_sum(*nums):
    total = 0
    for n in nums:
        total += n
    return total

print(my_sum(1, 2, 3))      # 6
print(my_sum(1, 2, 3, 4, 5)) # 15
```

**题目2：写一个判断奇偶的函数**

```python
def is_even(n):
    return n % 2 == 0

print(is_even(4))  # True
print(is_even(5))  # False
```

**题目3：写一个计算阶乘的函数**

```python
def factorial(n):
    result = 1
    for i in range(1, n + 1):
        result *= i
    return result

print(factorial(5))  # 120
```

**题目4：创建一个矩形类**

```python
class Rectangle:
    def __init__(self, width, height):
        self.width = width
        self.height = height
    
    def area(self):
        return self.width * self.height
    
    def perimeter(self):
        return 2 * (self.width + self.height)

rect = Rectangle(3, 4)
print(rect.area())      # 12
print(rect.perimeter()) # 14
```

**题目5：创建一个银行账户类**

```python
class BankAccount:
    def __init__(self, balance=0):
        self.balance = balance
    
    def deposit(self, amount):
        self.balance += amount
        print(f"存款{amount}，余额{self.balance}")
    
    def withdraw(self, amount):
        if amount > self.balance:
            print("余额不足")
        else:
            self.balance -= amount
            print(f"取款{amount}，余额{self.balance}")

account = BankAccount(100)
account.deposit(50)   # 存款50，余额150
account.withdraw(30)  # 取款30，余额120
```

---

### 五、进阶练习

**题目1：递归求阶乘**

```python
def factorial(n):
    if n == 1:
        return 1
    return n * factorial(n - 1)

print(factorial(5))  # 120
```

**题目2：写一个学生类，能计算平均成绩**

```python
class Student:
    def __init__(self, name, scores):
        self.name = name
        self.scores = scores
    
    def average(self):
        return sum(self.scores) / len(self.scores)

stu = Student("小明", [90, 85, 88])
print(stu.average())  # 87.66...
```

**题目3：写一个函数，判断字符串是否全是数字**

```python
def is_all_digits(s):
    return s.isdigit()

print(is_all_digits("12345"))  # True
print(is_all_digits("12a45"))  # False
```

---

## 总结

| 概念 | 要点 |
|------|------|
| 函数定义 | def + 参数 + return |
| 类定义 | class + __init__ + self |
| 参数 | 默认参数、可变参数 |
| 方法 | 类里的函数，第一个参数是self |

**记住**：
- 函数用 return 返回值
- 类的方法第一个参数是 self
- 调用函数要加括号

---

## 作业

1. 写一个函数，返回列表中的最大值
2. 写一个函数，判断一个数是否是质数
3. 创建一个圆类，能计算面积和周长
4. 创建一个计时器类，能记录时间差
5. 写一个递归函数，计算斐波那契数列第n项